# Plant!p 프로젝트 대시보드

> AI 중심 식물 위키백과 웹 서비스. 제10기 다빈치융합스쿨 탐구과제를 위한 식물 검색, 검증형 식물 상식, 나의 도감, 계정/프로필, 리프 보상, PWA 오프라인 지원을 포함한다.

## 한눈에 보기

| 항목 | 현재 상태 |
| --- | --- |
| 제품명 | Plant!p - AI 식물 위키백과 |
| 핵심 사용자 | 식물 탐구 과제를 수행하는 학생, 식물 키우기에 관심 있는 사용자 |
| 백엔드 | FastAPI |
| 프론트엔드 | 단일 HTML 템플릿 + Tailwind CDN + 브라우저 JS |
| 데이터 | Supabase Auth, plant_history, plant_directory, profiles, leaf_transactions |
| AI | Gemini API 사용 가능 시 생성, 미설정/실패 시 로컬 fallback |
| 앱 형태 | PWA manifest + service worker |
| 테스트 | 테스트 함수 88개 확인, 프로젝트 가상환경 테스트 명령 실행 성공 |

## 지금까지 된 것

- 식물명을 검색하면 기본 식물 지식, 학명, 물주기, 병충해, 상식 검증, 구매 링크를 보여준다.
- 비식물 검색어는 가드레일 메시지로 막는다.
- Supabase가 연결되면 로그인, 회원가입, 내 정보, 검색 히스토리 저장/삭제가 동작한다.
- plant_directory가 있으면 검증된 식물 데이터와 딥링크 구매 URL을 우선 사용한다.
- Gemini API가 없거나 실패해도 로컬 fallback으로 서비스가 멈추지 않는다.
- 나의 도감, 프로필, 리프 보상, 연속 출석 체크인 흐름이 들어가 있다.
- PWA 설치, 앱 셸 캐시, 오프라인 부팅을 위한 service worker가 있다.
- 스마트 물주기 대시보드와 이메일 알림 설정 UI가 준비되어 있다.

## 이번 주 우선순위

1. 스마트 물주기 기능을 실제 데이터 테이블과 연결한다.
2. 이메일/푸시 알림을 실제 발송 흐름으로 만든다.
3. 모바일에서 대시보드, 설정, 로그인 모달의 시각 QA를 마친다.
4. 환경변수와 배포 설정을 정리해서 제출/공유 시 민감값이 노출되지 않게 한다.
5. 프로젝트 소개 자료용 스크린샷과 데모 시나리오를 준비한다.

## 작업 보드

| 상태 | 우선순위 | 할 일 | 완료 기준 |
| --- | --- | --- | --- |
| 해야 함 | 높음 | 물주기 스케줄 API 만들기 | `plant_watering_schedules`를 조회/생성/수정/삭제하는 API가 있고, 사용자별 RLS가 유지된다. |
| 해야 함 | 높음 | 물주기 완료 기록을 히스토리 검색 재사용이 아닌 전용 흐름으로 분리 | “물 줬어요”를 눌렀을 때 마지막 물준 날짜와 다음 물줄 날짜가 정확히 갱신된다. |
| 해야 함 | 높음 | 알림 토글을 실제 저장값과 연결 | 아침 알림/즉시 알림 설정이 새로고침 후에도 유지된다. |
| 해야 함 | 높음 | 이메일 또는 브라우저 푸시 발송 방식 결정 | 어떤 채널을 쓸지 정하고, 최소 1개 채널의 실제 발송 테스트가 통과한다. |
| 해야 함 | 높음 | `.env` 관리 점검 | 민감 설정값을 문서/저장소/공유본에 노출하지 않고, 예시 파일만 남긴다. |
| 해야 함 | 중간 | 모바일 화면 QA | 375px, 768px, 데스크톱에서 검색/도감/대시보드/설정이 겹침 없이 보인다. |
| 해야 함 | 중간 | 오프라인 모드 UX 확인 | 네트워크가 없어도 첫 화면이 열리고, API 실패 시 사용자가 이해할 수 있는 상태가 보인다. |
| 해야 함 | 중간 | Google OAuth 설정 확인 | Supabase 대시보드의 Google Provider가 켜져 있고 redirect URL이 실제 배포 URL과 맞는다. |
| 해야 함 | 중간 | 식물 디렉터리 데이터 확장 | 대표 식물 50종 이상에 학명, 상식 검증, 구매 링크가 들어간다. |
| 해야 함 | 중간 | 제출용 데모 스크립트 작성 | 3분 안에 검색, 도감, 로그인, 물주기 대시보드, PWA 설치 포인트를 보여줄 수 있다. |
| 나중에 | 낮음 | 리프 사용처 설계 | 리프를 어디에 쓸지 정하고, 보상 경제가 과하지 않게 정리한다. |
| 나중에 | 낮음 | 관리자용 plant_directory 편집 화면 | 코드나 SQL을 직접 만지지 않고 식물 데이터를 추가/수정할 수 있다. |

## 내가 꼭 봐야 할 것

### 1. 실제 발표/제출에서 보여줄 핵심 흐름

- 토마토 또는 바질 검색
- “AI 상식 검증” 결과 확인
- 구매 링크가 실제 상세 상품으로 이동하는지 확인
- 로그인 후 “나의 도감”에 기록이 쌓이는지 확인
- 대시보드에서 물주기 D-Day가 보이는지 확인
- PWA 설치 버튼 또는 앱 설치 가능 상태 확인

### 2. 위험 포인트

- `.env` 파일에 실제 서비스 설정값이 있으므로 공유 전에 반드시 제외해야 한다.
- 프론트엔드가 한 파일에 길게 들어 있어 작은 수정도 화면 전체에 영향을 줄 수 있다.
- 물주기/알림 UI는 준비되어 있지만, 실제 발송과 스케줄 저장은 추가 구현이 필요하다.
- 외부 CDN(Tailwind, Pretendard)에 의존하므로 완전 오프라인 첫 방문 UX는 제한될 수 있다.
- 구매 링크는 외부 쇼핑몰 URL이라 깨질 수 있으므로 발표 전 대표 식물 몇 개는 직접 클릭 확인이 필요하다.

### 3. 확인 질문

- 최종 제출은 로컬 실행, 배포 URL, 영상 시연 중 무엇으로 할 것인가?
- 알림은 이메일이 중요한가, 브라우저 푸시가 중요한가?
- Plant!p의 핵심 차별점은 “AI 식물 위키”, “상식 검증”, “도감/물주기 관리” 중 무엇으로 강조할 것인가?
- 식물 데이터는 몇 종까지 준비하면 충분한가?

## 다음 구현 계획

### 1단계: 실제 물주기 데이터 연결

- `plant_watering_schedules` API 추가
- 검색 히스토리와 물주기 스케줄의 관계 정리
- 대시보드가 실제 스케줄 데이터를 읽도록 변경
- 테스트 추가: 생성, 조회, 갱신, 다른 사용자 접근 차단

### 2단계: 알림 기능 실사용화

- 알림 설정 저장 필드 확정
- 이메일 또는 푸시 중 1개 채널 먼저 구현
- 실패 로그와 마지막 발송 시각 저장
- 테스트용 “알림 보내보기” 버튼 추가

### 3단계: 제출 품질 다듬기

- 모바일 QA
- 깨진 링크/오타/긴 텍스트 넘침 확인
- 대표 식물 검색 시나리오 5개 고정
- 발표용 스크린샷과 시연 순서 작성

## 데모 시나리오

1. 첫 화면에서 Plant!p 로고와 검색창을 보여준다.
2. `토마토`를 검색해서 학명, 재배 정보, 병충해, 상식 검증을 보여준다.
3. “토마토는 물을 많이 줄수록 열매가 커진다” 같은 상식을 AI가 검증한다는 점을 설명한다.
4. 로그인 후 나의 도감에 검색 기록이 저장되는 흐름을 보여준다.
5. 대시보드에서 물주기 D-Day와 알림 설정 UI를 보여준다.
6. PWA 설치/오프라인 지원으로 웹앱처럼 쓸 수 있다는 점을 마무리로 강조한다.

## 완료 체크리스트

- [ ] Supabase 스키마 최신본 적용
- [ ] `.env` 공유 제외 확인
- [ ] 대표 식물 5개 검색 성공
- [ ] 로그인/회원가입 성공
- [ ] 히스토리 저장/삭제 성공
- [ ] 물주기 D-Day 실제 데이터 연결
- [ ] 알림 설정 저장
- [ ] 모바일 화면 확인
- [ ] 발표용 데모 스크립트 완성
- [ ] 제출 전 전체 테스트 실행

